package io.webagent4j.observation;

/** Semantic element property reported by the bounded MVP diff. */
public enum ChangedProperty {
    ROLE,
    ACCESSIBLE_NAME,
    TEXT,
    VISIBLE,
    ENABLED,
    EDITABLE,
    READ_ONLY,
    CHECKED,
    SELECTED,
    FOCUSED,
    EXPANDED,
    VALUE,
    CAPABILITIES,
    FORM_RELATIONSHIP
}
