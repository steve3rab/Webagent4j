package io.webagent4j.observation;

/** Action capability that is reliably supported by an observed semantic element. */
public enum ElementCapability {
    CLICK,
    TYPE,
    CLEAR,
    SELECT,
    CHECK,
    UNCHECK,
    FOCUS,
    SUBMIT,
    EXPAND,
    COLLAPSE
}
