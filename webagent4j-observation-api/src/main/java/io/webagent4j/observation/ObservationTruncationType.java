package io.webagent4j.observation;

/** Type of explicit budget truncation applied to an observation. */
public enum ObservationTruncationType {
    ELEMENTS,
    TREE_DEPTH,
    TEXT,
    TABLE_ROWS,
    TABLE_COLUMNS,
    LIST_ITEMS,
    SELECT_OPTIONS
}
