services:
  tests:
    profiles: [test]
    build:
      context: .
      target: test

  cli:
    profiles: [tooling]
    build:
      context: .
    command: ["version"]

# Integration tests use a local in-process HTTP server and need no external services.
