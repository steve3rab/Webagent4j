# Extraction

The extraction module currently establishes a one-way dependency on the DOM contract. It intentionally
does not publish schemas or converters until the CSS extraction vertical is implemented with type
conversion tests, failure semantics, and documentation. Planned values include strings, numbers,
dates, URIs, enums, and user-defined objects.
