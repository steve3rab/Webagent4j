# Workflow

The workflow module is reserved above the action contract. Its first implementation will compose tested
open, click, type, wait, and verify steps with variables, masked secrets, simple conditions, and a
structured workflow result. It will not become a general programming language, and definitions will
remain independent of Playwright.
